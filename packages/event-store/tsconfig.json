{
  "extends": "../../tsconfig.base.json",
  // Both halves are in here: a test `tsc` never reads is a test that can go
  // stale without saying so. `integration/sqlite.test.ts` is the whole of what
  // holds the SQLite store to the contracts (#178), and it was invisible here
  // until this line.
  "include": ["src", "unit", "integration", "test", "test-support", "prisma.config.ts"]
}
